# Personal Skills

`personal/` holds skills layered on this fork. It is **not** part of the upstream monorepo — never include `personal/` in PRs to `IgorWarzocha/howaboua-pi-stuff`.

- `skills/pi-setup-maintenance/` — runbook for this whole setup: layout, updates, changes, bootstrapping, switching to npm.

Agents: load `/skill:pi-setup-maintenance` before touching anything described there.
